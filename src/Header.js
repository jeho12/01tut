const Header = () => {
    const headerStyle = {
        backgroundColor: 'royalblue',
        color:'#fff'
    };

  return (
    <header style={headerStyle}>
        <h1>Groceries list</h1>
    </header>
  )
}

export default Header;